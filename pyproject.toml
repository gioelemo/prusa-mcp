[project]
name = "prusa-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.18.0",
    "playwright>=1.40.0",
    "python-dotenv>=1.0.0",
]
